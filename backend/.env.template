# Database Configuration
# Replace with your actual database URL (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/edwl_db?schema=public"

# Security
# Generate a strong, unique secret key for JWT
JWT_SECRET="your_seven_days_jwt_secret_key_here"

# Server Configuration
PORT=5000
NODE_ENV=development # change to production for deployment
CORS_ORIGIN=http://localhost:3000 # Allowed frontend origin (use '*' for all, or specific URL)

# Optional: Logging
# DEBUG=express:*
